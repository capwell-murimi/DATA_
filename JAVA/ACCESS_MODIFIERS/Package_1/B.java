package ACCESS_MODIFIERS.Package_1;

public class B {
    //A protected variable can be accessed by others but controlled(only in the same package and subclasses of other packages)
   protected String proc = "This is a protected string";
}
